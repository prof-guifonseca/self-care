{
  "name": "self-care",
  "version": "1.0.0",
  "description": "SelfCare PWA • Check-in de humor, dicas e citações (Functions + Offline)",
  "type": "module",
  "author": "NCS",
  "license": "ISC",

  "scripts": {
    "dev":   "netlify dev --port 8888",
    "start": "serve -s src",
    "lint":  "prettier --check \"**/*.{js,html,css,json}\"",
    "format": "prettier --write \"**/*.{js,html,css,json}\"",
    "test":  "echo \"(tests pendentes)\""
  },

  "keywords": ["self-care", "PWA", "Netlify", "JavaScript"],

  "devDependencies": {
    "netlify-cli": "^17.22.0",
    "prettier": "^3.3.1",
    "serve": "^14.2.4"
  }
}
